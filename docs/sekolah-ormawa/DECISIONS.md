# Keputusan Arsitektur dan Pertanyaan Terbuka

## Keputusan final Opsi A

| ID | Status | Keputusan | Konsekuensi |
|---|---|---|---|
| ADR-001 | FINAL | Sekolah adalah repository standalone pada path kanonis workspace | Root repository langsung menjadi aplikasi; tidak ada `apps/sekolah` |
| ADR-002 | FINAL | Domain produksi `sekolah.ormawaeksekutifpku.com` | DNS/TLS dan deployment independen |
| ADR-003 | FINAL | PostgreSQL, migration, storage, environment, dan release cycle mandiri | Tidak ada schema/foreign key/runtime dependency Nexus-Tevo |
| ADR-004 | FINAL | Auth internal untuk MVP | User, password hash, session, reset, revoke, dan audit dimiliki Sekolah |
| ADR-005 | FINAL | Role minimum `SUPER_ADMIN` dan `DEPT_PJ` | Role lain tidak dibuat tanpa revisi keputusan |
| ADR-006 | FINAL | Setiap `DEPT_PJ` memiliki satu department scope wajib | Backend memaksa scope dari session, bukan request client |
| ADR-007 | FINAL | Argon2id untuk password | Salt acak, parameter dibenchmark, tidak ada plaintext/legacy hash |
| ADR-008 | FINAL | Temporary password wajib diganti pada login pertama | Akses admin dibatasi hingga password berubah |
| ADR-009 | FINAL | Session database dapat dicabut | Logout, reset, disable, revoke-one/revoke-all, dan expiry menghapus atau menginvalidasi row session Better Auth |
| ADR-010 | FINAL | Super Admin mengelola lifecycle akun PJ | Create/edit/disable/reset/revoke wajib ter-audit |
| ADR-011 | FINAL | SSO Nexus di luar scope MVP | Hanya dapat masuk melalui future ADR dan migration plan tersendiri |
| ADR-012 | FINAL | Nexus-Tevo hanya referensi organisasi/visual | Data disalin sebagai seed/master lokal setelah divalidasi |
| ADR-013 | FINAL | Dokumen kandidat berada di private object storage | Tidak ada dokumen di `public/` atau URL publik permanen |
| ADR-014 | FINAL | Seluruh authorization ditegakkan backend | UI hanya mencerminkan hasil policy, bukan kontrol keamanan |
| ADR-015 | FINAL - Phase 1 | Gunakan Better Auth dengan Prisma adapter | Dokumentasi resmi menyatakan kompatibel dengan Next.js 16; menyediakan email/password, database session, reset password, rate limit, dan admin session controls |
| ADR-016 | FINAL - Phase 1 | Override password hashing Better Auth dengan Argon2id melalui `@node-rs/argon2` | Memenuhi keputusan Argon2id tanpa membangun credential flow dari nol |
| ADR-017 | FINAL - Phase 1, PARTIALLY SUPERSEDED | Ikuti schema auth Better Auth lalu tambahkan field domain Sekolah | Password/session tetap mengikuti library; keputusan reset `verifications` diganti ADR-023 |
| ADR-018 | FINAL - Phase 3 | Pendaftaran memiliki release gate terpisah dan fail-closed | `REGISTRATION_SUBMISSION_ENABLED` default `false`; status OPEN saja tidak cukup membuka form/submit |
| ADR-019 | FINAL - Phase 3 | Draft browser hanya menyimpan teks dengan schema, period, dan expiry | File, upload reference, token, dan consent tidak masuk `localStorage`; consent selalu diminta ulang |
| ADR-020 | FINAL - Phase 3 | Portofolio dimodelkan setelah candidate dibuat | `CandidatePortfolio` menunjuk kandidat serta tepat satu `fileUploadId` atau `externalUrl`; draft file dimiliki owner token hash |
| ADR-021 | FINAL - Phase 3 | Submit, nomor registrasi, outbox, audit, dan idempotency disatukan secara transaksional | Replay key/payload identik mengembalikan hasil awal; response secret disimpan terenkripsi; email gagal tidak membatalkan kandidat |
| ADR-022 | FINAL - Phase 4 | Better Auth tetap engine, seluruh policy sensitif dimiliki route aplikasi | Endpoint auth mentah ditutup; policy tidak dapat dilewati |
| ADR-023 | FINAL - Phase 4 | Reset memakai token hash aplikasi | `password_reset_tokens` single-use menggantikan sumber kebenaran `verifications` |
| ADR-024 | FINAL - Phase 4 | Rate limit dan revocation atomik di PostgreSQL | HMAC identity/IP, counter conflict-safe, dan session version |
| ADR-025 | FINAL - Phase 4 | Authorization backend; resource luar scope menjadi 404 | Role/scope tidak diterima dari client dan denial ter-audit |
| ADR-026 | FINAL - Phase 5 | Kandidat yang terkunci Birdep lain hilang total dari dashboard Birdep lain | Segmen primary/secondary/locked tidak menampilkan kandidat lintas Birdep begitu status menjadi `LOCKED` oleh Birdep lain; mengurangi enumeration, konsisten ADR-025 |
| ADR-027 | FINAL - Phase 5 | File viewer dashboard memakai policy role/scope backend, bukan owner-token HMAC | Endpoint `api/admin/candidates/[id]/files/[fileId]` mengautorisasi tiap request dari session admin; menggantikan pola Fase 3 yang khusus untuk draft publik |
| ADR-028 | FINAL - Phase 5 | Super Admin meninjau kandidat lewat satu Birdep terpilih (department switcher), bukan overview lintas-Birdep | Overview/manajemen lintas-Birdep, akun, periode, override, export, dan broadcast tetap Phase 7 (ADM-02) |
| ADR-029 | FINAL - Phase 5 | Catatan Birdep bersifat scoped per-department, bukan per-pembuat | PJ manapun dalam Birdep yang sama dapat mengubah/menghapus (soft-delete) catatan Birdep tersebut, konsisten dengan permission `note.manage.own_birdep` yang berskala Birdep |
| ADR-030 | FINAL - Phase 6 | Lock hanya diizinkan saat periode `OPEN`; unlock diizinkan saat `OPEN` atau `CLOSED` (ditolak saat `ARCHIVED`) dan mensyaratkan `allowUnlock=true` pada periode | Keputusan eksplisit pemilik proyek; lock berjendela lebih sempit daripada unlock - klaim kandidat berhenti begitu periode ditutup, sedangkan unlock tetap tersedia untuk koreksi administratif |
| ADR-031 | FINAL - Phase 6 | `lockReason`, `unlockReason`, dan `overrideReason` seluruhnya wajib diisi | Keputusan eksplisit pemilik proyek; `overrideReason`/aksi override lock baru dibangun Phase 7, tetapi kebijakan wajib-nya dikunci sekarang agar konsisten |
| ADR-032 | FINAL - Phase 8 | Period create (membuat periode baru dari nol) ditunda sampai setelah Phase 8 | Keputusan eksplisit pemilik proyek; edit periode dari seed (Phase 7) sudah cukup untuk MVP |
| ADR-033 | FINAL - Phase 8 | Browse/search kandidat lintas-Birdep penuh untuk Super Admin tidak dibangun untuk MVP | Keputusan eksplisit pemilik proyek; department switcher + daftar locked (Phase 7) sudah cukup |
| ADR-034 | FINAL - Phase 8 | State machine transisi status periode (validasi transisi DRAFT/OPEN/CLOSED/ARCHIVED yang masuk akal) dicatat sebagai technical debt post-MVP, tidak dibangun | Keputusan eksplisit pemilik proyek; operator bertanggung jawab memilih transisi yang benar sampai state machine dibangun |
| ADR-035 | FINAL - Phase 9 | Production private storage adalah MinIO self-hosted di server Contabo milik Biro, bukan Supabase Storage | Menggantikan rekomendasi Supabase di §Rekomendasi private storage; diakses lewat S3 API (MinIO S3-compatible) via `@aws-sdk/client-s3`; bucket privat tanpa akses publik/anonim, `put`/`read`/`delete` dan `createSignedUpload`/`createSignedDownload` (presigned URL 5 menit default, `STORAGE_SIGNED_URL_TTL_SECONDS`) diautentikasi kredensial aplikasi sendiri |
| ADR-036 | FINAL - Phase 9 | Production email adalah Resend (resend.com) lewat SDK/HTTP API, bukan SMTP langsung | `RESEND_API_KEY`/`RESEND_FROM_EMAIL` dari environment variable; menggantikan `LocalEmailSinkAdapter` development-only untuk `NODE_ENV=production` |
| ADR-037 | FINAL - Phase 9 | Deployment target production adalah self-hosted Docker Compose di server Contabo milik Biro, bukan Vercel/PaaS | Nginx reverse proxy, TLS lewat Let's Encrypt/certbot, environment variable dari file `.env` di server (bukan platform secret manager pihak ketiga); lihat RUNBOOK.md §Deployment readiness |
| ADR-038 | FINAL - Phase 9 | Owner domain/hosting/backup production adalah pemilik proyek sendiri, menggunakan server Contabo milik Biro | Menutup blocking item #5 di §Keputusan yang masih blocking |
| ADR-039 | FINAL - Phase 9 | Port Postgres Docker development permanen di `15432` (bukan `5432`/`55432`) | Windows Hyper-V/WinNAT mengunci rentang port dinamis yang menabrak port sebelumnya; `docker-compose.yml`, `.env`, `.env.example` konsisten memakai `15432` |
| ADR-040 | FINAL - UAT feedback | Field IPK dihapus dari form pendaftaran, dashboard PJ, dan export CSV | Keputusan eksplisit pemilik proyek dari UAT lokal; peserta Angkatan 63 mendaftar sebagai mahasiswa baru sebelum semester pertama selesai, sehingga belum memiliki IPK. `Candidate.gpa` dijadikan nullable (bukan di-drop) di schema Prisma - pilihan paling aman untuk data kandidat yang sudah ada; CHECK constraint `candidates_gpa_check` (0.00-4.00) tetap berlaku untuk baris manapun yang masih punya nilai gpa. Draft schema version pendaftaran dinaikkan ke 4 agar draft localStorage lama (masih berisi field gpa) tidak dipulihkan dalam bentuk usang. |
| ADR-041 | FINAL - Perubahan Sistem Seleksi | Sistem lock/unlock sederhana (`CandidateLock`) diganti `SelectionDecision` - keputusan bertahap antar PJ Pilihan 1 dan Pilihan 2 (`PENDING`/`TAKEN`/`HESITANT_P1`/`FORWARDED`/`TAKEN_P2`/`HESITANT_P2`/`ELIMINATED`). `CandidatePlacement` (tracking mentor/status penempatan terpisah) TIDAK dibangun ulang mengikuti sistem baru untuk MVP - status `TAKEN`/`TAKEN_P2` pada `SelectionDecision` dianggap representasi "sudah ditempatkan" | Keputusan eksplisit pemilik proyek. `candidate_locks`/`CandidateLock` dipertahankan (tidak dihapus) untuk referensi historis dan sebagai fallback visibilitas kandidat pra-migrasi yang belum memiliki baris `SelectionDecision` (lihat OR fallback di `segmentWhere()`/`findScopedCandidateId()`). Sebagai konsekuensi, alur `CandidatePlacement` (mentor label, catatan penempatan) menjadi tidak terjangkau untuk kandidat yang diproses lewat sistem baru, karena `updateCandidatePlacement` mensyaratkan baris `CandidateLock` aktif yang tidak lagi dibuat oleh `selection.ts` - **dicatat sebagai technical debt post-MVP**, bukan bug; keputusan membangun placement tracking terpisah yang mengikuti `SelectionDecision.status` ditunda sampai ada kebutuhan produk yang jelas. Men-supersede sebagian visibility rule ADR-026 (lock lama) untuk Birdep Pilihan 1/Pilihan 2. |
| ADR-042 | FINAL - Fix race test F6-01/SEL-04 | Root cause SEBENARNYA dari flakiness test 50-way race (`candidate-lock.test.ts` F6-01, `selection-decision.test.ts` SEL-04) BUKAN pool koneksi kelelahan atau I/O Docker Desktop seperti dugaan awal - **bug nyata**: `writeLockAudit`/`writeSelectionAudit` menulis baris audit lewat client `prisma` GLOBAL dari DALAM `prisma.$transaction()` yang masih terbuka, alih-alih lewat `tx` milik transaksi itu sendiri. Di bawah concurrency tinggi (50 request bersamaan pada satu baris), banyak transaksi yang kalah race menahan koneksi masing-masing sambil menunggu row-lock milik si pemenang; si pemenang lalu butuh SATU KONEKSI TAMBAHAN dari pool yang sama untuk menulis audit sebelum bisa commit - kalau pool sudah habis oleh transaksi-transaksi yang justru menunggu si pemenang, terjadi self-deadlock murni (bukan lambat, benar-benar buntu sampai `$transaction` timeout). Dibuktikan lewat reproduksi terisolasi (raw `pg`: 438ms sehat; Prisma `$transaction` biasa: ~1s sehat; ditambah SATU panggilan audit lewat client global saat transaksi terbuka: langsung deadlock 30s persis meniru gejala asli) sebelum kode produksi diubah. **Fix**: `writeAuditLog`/`writeAuthAudit` (`src/server/auth/audit.ts`) sekarang menerima parameter `client` opsional (default client global untuk pemanggil yang TIDAK di dalam transaksi - mayoritas pemanggil lain di codebase sudah benar/tidak terpengaruh); `writeLockAudit` (`lock.ts`) dan `writeSelectionAudit` (`selection.ts`) mengoper `tx` miliknya - parameter `client` di `writeSelectionAudit` dibuat WAJIB (bukan opsional) karena semua pemanggilnya selalu di dalam transaksi, mencegah bug yang sama terulang tanpa sengaja. Hasil: race test F6-01+SEL-04 dari sebelumnya sering gantung/gagal ~30s turun jadi lulus konsisten di bawah 1 detik masing-masing. | Keputusan eksplisit pemilik proyek untuk memperbaiki root cause, bukan hanya menaikkan angka timeout. Sebagai pengaman tambahan (bukan fix utama) - juga ditambahkan: `DATABASE_CONNECTION_LIMIT` (pool size Prisma client, default 20, test override ke 10 - `src/lib/db.ts`/`src/lib/env.ts`), `disconnectPrismaForTests()` (test-only helper yang menutup DAN membersihkan cache client Prisma dev-mode antar file test, karena Vitest bisa memakai ulang satu worker untuk banyak file tanpa reset `globalThis` - client yang sudah `$disconnect()` tapi masih ke-cache membuat file berikutnya memakai client mati), dan `PRISMA_TRANSACTION_TIMEOUT_MS` (durasi `timeout` transaksi `$transaction()` di `lock.ts`/`selection.ts`, dibaca dari environment alih-alih hardcode - default 15000ms sama seperti sebelumnya untuk dev/prod, integration test suite menaikkan ke 30000ms sebagai headroom ekstra). Dugaan awal "Windows Docker Desktop I/O yang melambat di akhir suite panjang" TIDAK terbukti benar setelah investigasi - container Postgres yang di-restart dan raw-`pg` probe tetap sehat/cepat sepanjang waktu; faktor Windows/Docker Desktop TETAP relevan sebagai karakteristik lingkungan pengembangan ini secara umum (lihat riwayat outage Docker Desktop di sesi-sesi sebelumnya), tetapi bukan penyebab kegagalan spesifik ini - dicatat di sini agar sesi mendatang tidak mengulang jalur investigasi yang sama. |
| ADR-043 | FINAL - Phase C, Field Khusus Per Birdep | Field tambahan per-Birdep (MBTI Komit, fokus Adkesmah, portofolio Badan Media dan Branding, RAB Komisi Anggaran) disimpan lewat DUA mekanisme berbeda, bukan satu tabel/JSON generik: (1) tabel supplementary 1:1 baru `CandidateSupplementalData` (`candidate_supplemental_data`, `candidateId` unique, kolom `komitMbti String?` dan `adkesmahFocus AdkesmahFocus?`) untuk field teks/pilihan yang TIDAK melibatkan file, mengikuti persis pola `CandidatePlacement`/`SelectionDecision` yang sudah ada; (2) field portofolio Badan Media dan Branding dan RAB Komisi Anggaran TIDAK mendapat tabel baru - keduanya memakai ulang mekanisme `CandidatePortfolio`/`FileUpload` yang sudah ada, karena keduanya murni upload file dan mekanisme itu sudah menangani validasi MIME/magic-byte/ukuran serta private storage. Portofolio Badan Media dan Branding memakai `UploadKind.PORTFOLIO` yang sama dengan Medbrand eksekutif (policy-nya diperluas, lihat konsekuensi); RAB Komisi Anggaran mendapat `UploadKind.BUDGET_PLAN` baru karena kebijakannya (opsional, PDF/XLS/XLSX, 5MB) berbeda dari `PORTFOLIO`. **SUPERSEDED SEBAGIAN oleh ADR-045 (Phase D)**: bagian (2) - mekanisme upload file untuk ketiga field ini - diganti total menjadi input URL Google Drive; bagian (1) tetap berlaku dan malah menyerap field URL tersebut. | Ditolak: JSON blob di kolom `Candidate` (tidak type-safe, tidak bisa di-index/constraint per field, menyimpang dari konvensi schema yang selalu memakai kolom bertipe eksplisit) dan tabel EAV generik `key`/`value` (kehilangan type safety yang sama, dan tidak ada preseden EAV di schema ini). Konsekuensi migrasi: `UploadKind` mendapat nilai baru `BUDGET_PLAN` (tidak bisa di-drop lewat `ALTER TYPE` jika suatu saat perlu rollback - didokumentasikan di komentar migration.sql). Konsekuensi kebijakan file: policy `PORTFOLIO` di `file-validation.ts` diperluas dari gambar-saja (~5MB) menjadi PDF/ZIP/gambar hingga 10MB untuk MENYAMAKAN dengan spesifikasi Badan Media dan Branding - ini juga otomatis berlaku untuk Medbrand eksekutif (diperlakukan sebagai pelonggaran input yang aman, bukan pengetatan, sehingga tidak ada submission valid yang sebelumnya lolos menjadi ditolak). `detectMimeType()` diperluas mengembalikan lebih dari satu kandidat MIME karena XLSX dan ZIP berbagi magic byte `PK\x03\x04` yang sama - pemanggil memutuskan valid/tidak berdasarkan MIME yang dideklarasikan klien dicocokkan terhadap himpunan kandidat, bukan satu nilai tunggal. **Lihat ADR-045 untuk keputusan Phase D yang mengubah sebagian keputusan ini.** |
| ADR-044 | FINAL (retrospektif, disusulkan di Phase D) - Phase A/B, Jalur Legislatif | Organisasi dibagi dua jalur (`Track` enum: `EXECUTIVE`/`LEGISLATIVE`) di level `Department` (default `EXECUTIVE`, DB-level default) dan `Candidate` (wajib diisi, SENGAJA tanpa DB-level default supaya setiap `candidate.create()` dipaksa compiler mengisinya eksplisit). Kedua pilihan kandidat wajib berasal dari track yang sama - ditegakkan dua lapis: `validateRegistrationPayload` (fast-fail terhadap config snapshot client) dan `submit.ts` (authoritative terhadap data DB segar). Form pendaftaran mendapat Step 0 baru (pemilihan jalur) sebelum Step 1 Identitas; Step 2 memfilter Birdep sesuai jalur lewat `GET /api/departments` (route publik baru, dikelompokkan `{executive, legislative}`) - bukan `config.departments` yang di-scope ke `acceptsApplications` periode berjalan. | Keputusan eksplisit pemilik proyek. BPH tetap dikecualikan dari seluruh listing department candidate-facing (konsisten, bukan pengecualian baru). 5 unit legislatif baru (KOMLEG, KOMANGG, KOMPENG, BADINTEKST, BADMEDBRND) di-seed lewat `prisma/seed.ts` (idempotent), bukan lewat `prisma db seed` langsung terhadap dev DB yang sudah dimutasi manual (akan me-reset `acceptsApplications`/password fixture UAT) - dipakai script sekali-pakai tambahan lalu dihapus. ADR ini ditulis retrospektif di Phase D karena PHASE_STATUS.md/DECISIONS.md tidak diperbarui saat Phase A/B asli dikerjakan - lihat laporan "Hasil Phase A"/"Hasil Phase B" di `PHASE_STATUS.md` untuk detail lengkap. |
| ADR-045 | FINAL - Phase D, Portofolio via URL Google Drive | Menggantikan sebagian ADR-043: portofolio Medbrand eksekutif/Badan Media dan Branding legislatif dan RAB Komisi Anggaran legislatif TIDAK LAGI file upload - ketiganya jadi field teks URL Google Drive biasa. `CandidatePortfolio`/`candidate_portfolios` (mekanisme Phase 3 asli) DI-DROP TOTAL dari schema - satu-satunya pemakainya adalah field-field ini, jadi tidak ada alasan mempertahankannya sebagai tabel kosong. Dua kolom baru `portfolioUrl String?` dan `budgetPlanUrl String?` ditambahkan ke `CandidateSupplementalData` (tabel yang sama yang dipakai `komitMbti`/`adkesmahFocus`, ADR-043) - field ini SEKARANG identik sifatnya (teks, tidak melibatkan file/storage), sehingga penyatuan ke satu tabel lebih konsisten daripada mekanisme file terpisah yang tadinya dipisah justru karena sifatnya berbeda. Validasi: wajib diawali `https://drive.google.com` (dicek lewat `URL.hostname === "drive.google.com"`, BUKAN string `startsWith()` naif, supaya tidak bisa dilewati trik subdomain seperti `https://drive.google.com.evil.test/...`), panjang maksimum tetap pakai `PORTFOLIO_URL_MAX_LENGTH` yang sudah ada. Instruksi "Anyone with the link can view" ditampilkan sebagai hint field, tidak diverifikasi server (tidak mungkin diverifikasi tanpa akses API Google Drive - dicatat sebagai keterbatasan yang diketahui, bukan disembunyikan). | Keputusan eksplisit pemilik proyek. Konsekuensi: `UploadKind.PORTFOLIO`/`BUDGET_PLAN` (ditambahkan ADR-043) DIPENSIUNKAN, bukan dihapus dari enum (Postgres tidak punya "drop satu nilai enum" - perlu rebuild tipe penuh; tidak dilakukan karena berisiko tanpa manfaat nyata untuk dua nilai yang sudah tidak dipakai) - `policyFor()` di `file-validation.ts` mengembalikan policy yang mustahil dipenuhi (allow-list kosong) untuk keduanya, dan endpoint upload (`POST /api/registration/uploads`) menolaknya di level Set kind yang diterima, bukan hanya mengandalkan policy. `detectMimeType()` disederhanakan kembali (ZIP/XLS/XLSX dihapus - satu-satunya pemakainya, PORTFOLIO/BUDGET_PLAN, sudah pensiun) - hanya mendeteksi PDF/JPEG/PNG, masih mengembalikan array untuk konsistensi API meski tidak lagi ambigu. Env var `PORTFOLIO_MAX_FILES`/`PORTFOLIO_MAX_FILE_BYTES`/`BUDGET_PLAN_MAX_FILE_BYTES` dihapus (mati total); `PORTFOLIO_URL_MAX_LENGTH` dipertahankan dan dipakai ulang. **Data hilang**: migration `20260906120000_portfolio_google_drive_url` men-DROP tabel `candidate_portfolios` tanpa migrasi data - satu baris fixture (kandidat sintetis "Wicaksa_Rafi Al", `d-0001`, dari live-testing Phase B sebelumnya) hilang permanen. Diterima karena submission production belum pernah diaktifkan (ADR-018) dan baris itu murni data sintetis/UAT, bukan data kandidat sungguhan. |
| ADR-046 | FINAL - UAT feedback, Bukti Follow dan Share | Persyaratan follow Instagram dan share (berlaku untuk SEMUA pendaftar, eksekutif maupun legislatif) diimplementasikan sebagai satu PDF wajib memakai ULANG mekanisme `FileUpload`/`UploadKind` yang sudah ada (`UploadKind.FOLLOW_EVIDENCE` baru), BUKAN kolom baru di `Candidate` (`followEvidenceObjectKey`/`followEvidenceOriginalName`) seperti yang ditawarkan sebagai opsi di spek. Field ini identik strukturnya dengan CV/PHOTO/STUDENT_CARD - satu file, satu owner, satu lifecycle - sehingga viewer dashboard PJ yang sudah ada dan sudah terautorisasi (`GET /api/admin/candidates/[id]/files/[fileId]`, ADR-027, kind-agnostic) langsung bisa menyajikannya tanpa perubahan kode sama sekali. Di dashboard PJ, field ini ditampilkan di kartu "Dokumen" generik yang sama dengan CV/Pas foto/KTM (lewat `UPLOAD_LABEL` map), bukan kartu khusus baru - visibilitas "hanya PJ Birdep terkait dan Super Admin" sudah persis cara kerja visibilitas CV/Photo/KTM yang sudah ada (siapapun PJ - P1 atau P2 - yang sudah bisa melihat kandidat ini, plus Super Admin), jadi tidak perlu primitive visibilitas baru. Step baru di form pendaftaran ("Bukti Follow dan Share", Step 04) ditempatkan setelah Dokumen dan sebelum Esai & Portofolio, TIDAK bersyarat department seperti field Phase C/D - selalu tampil untuk semua pendaftar. Policy: PDF only, maksimum 10MB (batas terbesar di antara CV/PHOTO/STUDENT_CARD/FOLLOW_EVIDENCE, sehingga `MAX_DOCUMENT_UPLOAD_BYTES` dinaikkan dari 2MB ke 10MB). | Keputusan eksplisit pemilik proyek, arsitektur dipilih sendiri sesuai instruksi ("pilihkan yang paling konsisten dengan arsitektur yang sudah ada"). Konsekuensi migrasi: `UploadKind` mendapat nilai baru `FOLLOW_EVIDENCE` (sama seperti `PORTFOLIO`/`BUDGET_PLAN` di ADR-043/045, tidak bisa di-drop lewat `ALTER TYPE` - didokumentasikan di komentar migration.sql). Kandidat fixture yang sudah ada (8 baris, dari sebelum fitur ini) di-backfill dengan `FileUpload` `FOLLOW_EVIDENCE` placeholder sintetis (`originalFileName: "PLACEHOLDER_bukti-follow-dan-share.pdf"`, status `FINALIZED`) beserta byte PDF placeholder nyata yang ditulis ke private storage development, lewat script sekali-pakai yang dihapus setelah dijalankan (pola yang sama dipakai Phase A/D) - tanpa ini, link viewer PJ untuk kandidat lama akan 404 karena baris DB menunjuk object storage yang tidak ada. |
| ADR-047 | FINAL - Guidebook, ketentuan, dan pembayaran | (1) `paymentCode`/`paymentAmount` jadi kolom langsung di `Candidate` (bukan `FileUpload`/tabel supplemental) sesuai instruksi eksplisit spek - keduanya nilai kecil bertipe statis, bukan file. (2) Bukti pembayaran (`PAYMENT_EVIDENCE`) tetap memakai ULANG mekanisme `FileUpload`/`UploadKind` seperti `FOLLOW_EVIDENCE` (ADR-046) - satu file, satu owner, satu lifecycle, viewer yang sama. (3) Kode unik 3-digit dibuat ATOMIK per periode lewat `RecruitmentPeriod.paymentCodeSequence` (pola identik `registrationSequence` yang sudah ada untuk nomor registrasi) - satu `UPDATE ... increment` per panggilan `GET /api/registration/payment-code`, dipanggil SEKALI saat registrant pertama kali masuk Step Pembayaran (bukan saat submit final) supaya kode bisa ditampilkan sebelum pembayaran dilakukan; hasilnya disimpan ke draft localStorage (`REGISTRATION_DRAFT_SCHEMA_VERSION` dinaikkan ke 5) sehingga tidak diterbitkan ulang saat reload/kembali ke step ini. (4) Endpoint tersebut sengaja TIDAK memanggil `assertValidCsrf()` seperti endpoint POST lain di direktori yang sama - `assertValidCsrf` mewajibkan header `Origin` hadir dan menolak jika tidak, yang andal untuk method "unsafe" (POST) tapi TIDAK untuk GET same-origin biasa (browser tidak selalu mengirim `Origin` untuk itu); endpoint ini juga tidak berbasis cookie/sesi sehingga tidak ada ambient authority untuk dieksploitasi CSRF sungguhan - mitigasi penyalahgunaan (menghabiskan ruang kode 3 digit) sepenuhnya lewat rate limit per-IP (`REGISTRATION_PAYMENT_CODE`, maksimum 20) dan validasi `periodId` terhadap periode yang benar-benar `OPEN`. (5) Path endpoint `/api/registration/payment-code` (singular, bukan `/api/registrations/...` seperti tertulis di spek) - mengikuti konvensi path yang sudah ada di seluruh direktori ini (`/api/registration/submit`, `/api/registration/uploads`), deviasi disengaja demi konsistensi. | Keputusan eksplisit pemilik proyek untuk (1)/(2); (3)-(5) dipilih sendiri sesuai instruksi ("pilihkan yang paling konsisten"). Konsekuensi migrasi: `Candidate.paymentCode`/`paymentAmount` NOT NULL (ditambahkan nullable, di-backfill kode sekuensial per periode via `ROW_NUMBER()` terurut `submittedAt`, baru diperketat NOT NULL - pola sama seperti kolom `track`), `@@unique([periodId, paymentCode])` (plain unique, BUKAN partial - berbeda dari instruksi spek "partial unique index" karena `paymentCode` selalu NOT NULL, tidak ada kasus NULL yang perlu dikecualikan seperti `CandidateLock`). `UploadKind` +`PAYMENT_EVIDENCE` (pensiun tidak bisa di-drop, sama seperti kind lain). **Kode tidak dijamin gapless** (registrant yang mengambil kode lalu tidak pernah submit meninggalkan gap) - diterima, sama seperti `registrationSequence`. **Format "3 digit" tidak dijamin permanen** - lewat 999 registrant per periode, kode jadi 4+ digit (tetap unik, tetap benar secara matematis) alih-alih mempertahankan format 3-digit persis; tidak diblok karena memblokir pendaftaran ke-1000 adalah keputusan bisnis di luar kewenangan implementasi ini. **Tidak ada backfill placeholder `PAYMENT_EVIDENCE`** untuk 8 kandidat fixture yang sudah ada (berbeda dari `FOLLOW_EVIDENCE` di ADR-046) - kartu "Pembayaran" dashboard PJ menampilkan "Belum ada bukti pembayaran" dengan aman (bukan link rusak) untuk kandidat tanpa file, jadi tidak ada risiko 404 yang perlu dicegah lewat backfill. |
| ADR-048 | FINAL - Perubahan Sistem Pembayaran (supersede ADR-047 sebagian) | Kode unik per pendaftar (ADR-047) DIBALIK total menjadi nominal tetap (Rp 15.001, `PAYMENT_AMOUNT`) untuk semua pendaftar - keputusan eksplisit pemilik proyek, bukan bug/regresi. `Candidate.paymentCode`/`paymentAmount` DI-DROP dari schema (bukan dijadikan nullable) karena tidak ada lagi nilai per-kandidat yang perlu disimpan - nominal sekarang murni konten statis (ditampilkan di form, bukan data). `@@unique([periodId, paymentCode])` ikut di-drop. `GET /api/registration/payment-code` dihapus total (route dan direktorinya). `RecruitmentPeriod.paymentCodeSequence` (counter atomik ADR-047) JUGA di-drop meski tidak eksplisit diminta di spek - begitu endpoint penerbit kode dan pemakaiannya di `submit.ts` dihapus, counter itu tidak lagi punya reader/writer sama sekali sehingga mempertahankannya berarti schema mati; didokumentasikan di sini sebagai satu-satunya deviasi dari instruksi literal. `PAYMENT_BASE_AMOUNT` (env var lama, basis + kode) di-rename `PAYMENT_AMOUNT` (nominal final langsung) - bukan sekadar reinterpretasi nilai lama, karena semantiknya benar-benar berubah (dulu "ditambah kode", sekarang "final apa adanya"). Bukti pembayaran (`PAYMENT_EVIDENCE`, upload) TIDAK disentuh - tetap wajib, mekanisme sama persis seperti ADR-047/ADR-046. Tampilan "Kode unik"/"Total dibayar" dihapus dari Step Pembayaran, kartu Pembayaran dashboard PJ/Super Admin, dan Review & Submit form (yang terakhir tidak eksplisit diminta di spek tapi jelas konsisten dengan semangat "hapus tampilan kode unik" di seluruh permukaan produk). | Keputusan eksplisit pemilik proyek untuk pembalikan sistem pembayaran; keputusan drop `paymentCodeSequence` dan penghapusan tampilan di Review & Submit dipilih sendiri karena konsisten dengan instruksi, bukan diminta kata demi kata - keduanya ditandai di laporan fase. **Data hilang** (preseden sama seperti ADR-045): migration `20260908080000_revert_payment_to_fixed_amount` men-DROP kolom `paymentCode`/`paymentAmount` tanpa migrasi data - 8 baris kandidat fixture (`paymentCode` 001-008, `paymentAmount` 15001-15008) kehilangan nilai itu permanen, tidak ada tempat migrasi yang bermakna karena skema baru tidak punya field per-kandidat sama sekali. Diterima karena submission production belum pernah diaktifkan (ADR-018) dan seluruh 8 baris adalah data fixture sintetis. `REGISTRATION_DRAFT_SCHEMA_VERSION` dinaikkan ke 6 (draft localStorage skema-5 lama membawa bentuk `payment.code`/`payment.amount` yang sudah tidak ada) - draft lama dibuang, bukan dipulihkan dalam bentuk usang, konsisten dengan pola ADR-040. |
| ADR-049 | FINAL - Tambah Role Baru dan 2 Akun (KETUA_PELAKSANA) | **Menyupersede sebagian ADR-005** ("Role minimum SUPER_ADMIN dan DEPT_PJ - role lain tidak dibuat tanpa revisi keputusan") - keputusan eksplisit pemilik proyek untuk merevisinya dengan role ketiga: `KETUA_PELAKSANA`, read-only lintas-Birdep (lihat semua kandidat/dashboard PJ lewat department switcher, export semua Birdep, kirim broadcast, lihat audit log) TANPA hak administratif (kelola periode/akun PJ/override lock/reset seleksi/soft delete-restore tetap eksklusif `requireSuperAdmin`, tidak disentuh). **Deviasi penting dari instruksi literal spek**: (1) spek meminta "tambah enum value KETUA_PELAKSANA di UserRole" - tidak ada Prisma enum `UserRole` di schema ini; `User.role` adalah `String` yang mengacu (FK) ke tabel generik `Role`/`Permission`/`RolePermission` (RBAC berbasis data, bukan enum Postgres) - jadi menambah role di sini murni DATA (upsert `prisma/seed.ts`'s `roleFixtures`/`permissionFixtures`), bukan `ALTER TYPE`. (2) SATU-SATUNYA perubahan skema yang benar-benar diperlukan (ditemukan lewat kegagalan integration test, bukan dugaan) adalah `users_role_department_scope_check` - CHECK constraint dari migration init yang berupa closed enumeration `(SUPER_ADMIN, departmentId IS NULL) OR (DEPT_PJ, departmentId IS NOT NULL)`; nilai role lain apa pun ditolak di level database terlepas dari apa yang diizinkan lapisan TypeScript. Migration `20260908090000_add_ketua_pelaksana_role` menambah disjunct ketiga `(KETUA_PELAKSANA, departmentId IS NULL)` - KETUA_PELAKSANA memakai invarian "tanpa departmentId" yang sama seperti SUPER_ADMIN (baca lintas-Birdep lewat switcher, bukan scope tetap di sesi), rollback didokumentasikan penuh di komentar migration.sql. Implementasi: 4 permission baru (`sekolah.candidate.read.all`, `sekolah.export.all`, `sekolah.broadcast.send`, `sekolah.logs.read`) ditambahkan di `SCHOOL_PERMISSIONS`/`rolePermissions` (`src/lib/auth/permissions.ts`) - route kandidat list/detail/file-viewer/export yang tadinya mengecek SATU permission `*.own_birdep` sekarang memakai `requireAnyPermission` (helper baru) supaya baris kode yang sama melayani baik DEPT_PJ (own) maupun KETUA_PELAKSANA (all); route broadcast send/preview yang tadinya `requireSuperAdmin` literal diganti `requirePermission(..., "sekolah.broadcast.send")` (SUPER_ADMIN tetap lolos otomatis karena tetap mendapat SEMUA permission). Helper baru `hasAllDepartmentAccess(role)` (true untuk SUPER_ADMIN dan KETUA_PELAKSANA) menggantikan literal `role === "SUPER_ADMIN"` di titik-titik yang maknanya sebenarnya "boleh akses department manapun" (`requireDepartmentAccess`, `resolveRequestDepartmentId`, sidebar/department-switcher dashboard) - titik yang maknanya benar-benar "harus persis SUPER_ADMIN" (`requireSuperAdmin` dan pemanggilnya: periode, akun PJ, override, delete/restore, locks) SENGAJA tidak disentuh. UI: menu "Kelola akun PJ"/"Periode & override lock" tetap `role === "SUPER_ADMIN"` eksplisit; menu "Broadcast" dan department switcher dashboard memakai `hasSchoolPermission(...)`/`hasAllDepartmentAccess(...)` supaya otomatis ikut mencakup KETUA_PELAKSANA. **"Lihat audit log" tidak diimplementasikan sebagai halaman/endpoint baru** - digrep penuh dan dikonfirmasi tidak ada satu pun route/halaman admin yang MEMBACA `AuditLog` di seluruh codebase ini (hanya ditulis, dari `selection.ts`/`lock.ts`/`accounts.ts`/dll) - membangun viewer audit log dari nol adalah fitur baru yang substansial, bukan sekadar meng-extend kontrol akses yang sudah ada, sehingga di luar cakupan "jangan mulai fase baru". Permission `sekolah.logs.read` tetap ditambahkan dan digrant ke KETUA_PELAKSANA+SUPER_ADMIN persis sesuai spek (siap dipakai begitu viewer-nya dibangun di fase terpisah), tapi TIDAK ADA satu pun kode yang mengonsumsinya saat ini - dicatat eksplisit di laporan fase, bukan disembunyikan. Bagian 2 (2 akun): `scripts/create-additional-accounts.ts` (script baru, bukan mengubah `create-pj-accounts.ts` yang sudah pernah dijalankan) - duplikasi logika `createPjAccount()` dengan alasan identik ADR sebelumnya (constraint `"server-only"` mencegah import langsung dari script `tsx` standalone), digeneralisasi mendukung akun tanpa department (KETUA_PELAKSANA) yang tidak bisa dilayani `createPjAccount()` aslinya (hardcode `role: "DEPT_PJ"` + department wajib). | Keputusan eksplisit pemilik proyek untuk role baru dan pembatasan hak aksesnya; deviasi (1)/(2)/audit-log-viewer di atas ditemukan dan dipilih sendiri sesuai prinsip "pilih yang paling konsisten dengan arsitektur yang sudah ada", ditandai jelas untuk ditinjau. Live-tested: script dijalankan 2x terhadap dev DB (create lalu re-run idempoten skip 2/2), lalu dibersihkan (bukan data produksi sungguhan - 2 email asli dari spek dipakai APA ADANYA sesuai instruksi, bukan email sintetis, sehingga baris uji coba dihapus total setelah verifikasi alih-alih dibiarkan sebagai fixture). `tests/integration/authorization-matrix.test.ts` diperluas dengan fixture KETUA_PELAKSANA (BISA: list/detail kandidat lintas-Birdep 200, broadcast preview 200; TIDAK BISA: akun/periode/override 403) dan `tests/unit/permissions.test.ts` diperluas dengan matriks permission langsung. |

## Dampak terhadap PRD awal

Keputusan pemilik proyek secara eksplisit menggantikan asumsi PRD berikut untuk MVP:

- struktur monorepo Nexus-Tevo-Sekolah;
- penggunaan akun, password, session, role, permission, atau membership Nexus;
- shared cookie/session package dan SSO handoff;
- panel Sekolah yang mendelegasikan reset akun ke Nexus;
- penggunaan schema/database yang sama dengan Nexus-Tevo.

Requirement produk lain - termasuk program untuk Angkatan 63, dua role admin, isolasi Birdep, lock concurrency-safe, private upload, audit, accessibility, dan phase gate - tetap berlaku.

## Evaluasi authentication library Phase 1

Better Auth `1.6.25` dipilih untuk Next.js `16.2.7`, dengan Prisma `7.9.1`, dibanding membangun auth manual atau memakai Credentials provider generik karena:

- kompatibel resmi dengan Next.js 16 dan Route Handler;
- memiliki Prisma adapter dan schema generator;
- menyediakan email/password, change/reset password, database session, revoke session, admin ban/unban, dan rate limit;
- mengizinkan fungsi hash/verify password kustom sehingga Argon2id dapat dipakai;
- additional user fields memungkinkan `mustChangePassword` dan `departmentId` disimpan pada user.

Batasan dan keputusan mitigasi:

- Prisma adapter hanya mendukung schema generation, bukan migration otomatis; migration tetap dibuat, ditinjau, dan dijalankan melalui Prisma.
- Password credential disimpan pada `accounts.password`, bukan `users.password_hash` seperti rancangan awal Phase 0.
- Token reset menggunakan tabel `verifications`, bukan tabel khusus `password_reset_tokens`.
- Session token disimpan sebagai token opaque pada tabel `sessions` sesuai schema library, bukan hash token. Database harus least-privilege, log wajib redact, cookie `HttpOnly`/`Secure`, dan revocation menghapus session row.
- Admin plugin memiliki ban/revoke controls, tetapi policy `SUPER_ADMIN`/`DEPT_PJ`, forced password, dan scope Birdep tetap ditegakkan guard server aplikasi.
- Rate limit bawaan diaktifkan juga pada development dan parameternya dibaca dari environment. Trust terhadap proxy IP header harus dikunci saat provider hosting dipilih.
- `updateAge` Better Auth adalah interval pembaruan rolling session, bukan hard idle timeout. Nama environment Phase 0 direvisi menjadi `SESSION_UPDATE_AGE_SECONDS`; hard idle policy, bila disetujui, harus dibuat dan diuji pada Phase 4.
- Callback pengiriman email reset belum diaktifkan karena provider/sender belum disetujui. Engine token reset sudah tersedia, tetapi fitur reset end-to-end tetap pekerjaan Phase 4.
- Plugin admin menyediakan primitive ban/unban dan revoke; UI serta workflow Super Admin baru dibangun pada Phase 7.

Pemetaan requirement keamanan ke fondasi yang dibuat:

| Requirement | Implementasi fondasi Phase 1 | Batas fase |
|---|---|---|
| Argon2id | Custom `hash`/`verify` memakai `@node-rs/argon2`, memory 64 MiB, time cost 3, parallelism 1 | Benchmark runtime final di Phase 4/8 |
| `mustChangePassword` | Field user tambahan dan server guard; semua akun fixture bernilai `true` | UI/alur ganti password pertama di Phase 4 |
| Account disable | Field `banned` Better Auth dan guard backend menolak user banned | UI lifecycle akun di Phase 7 |
| Session revocation | Database session dan admin revoke API; reset dikonfigurasi `revokeSessionsOnPasswordReset` | E2E lifecycle di Phase 4 |
| Reset password | Tabel `verifications`, TTL dari environment, callback reset membersihkan forced-password state | Pengiriman email belum aktif |
| Login rate limit | Better Auth database rate limit pada sign-in dan request reset | Tuning proxy/IP di Phase 8 |
| Role dan department scope | Custom access control, server guard, FK role, serta check constraint PostgreSQL | Test endpoint penuh di Phase 4-7 |

Referensi resmi: [Next.js integration](https://better-auth.com/docs/integrations/next), [email/password dan Argon2](https://better-auth.com/docs/authentication/email-password), [Prisma adapter](https://better-auth.com/docs/adapters/prisma), [admin session controls](https://better-auth.com/docs/plugins/admin), dan [rate limit](https://better-auth.com/docs/concepts/rate-limit).

## ADR-022 - Better Auth tetap menjadi engine; policy sensitif dimiliki aplikasi

Status: Accepted pada Phase 4.

Better Auth `1.6.25` tetap dipakai untuk credential account, Argon2id adapter, pembuatan/database session, cookie, dan primitive sign-in/sign-out. Endpoint mentah `/api/auth/*` sengaja ditutup; seluruh alur publik admin masuk melalui `/api/admin/auth/*` agar forced password, disable/ban, absolute/idle expiry, session version, rate limit gabungan, audit, dan response redaction tidak dapat dilewati.

Konsekuensi:

- password tetap berada pada `accounts.password` dalam format PHC Argon2id;
- token session opaque dikelola Better Auth dan tersimpan pada database library, sehingga least privilege dan redaction database/log tetap wajib;
- `users.isActive`, `users.sessionVersion`, `sessions.absoluteExpiresAt`, dan `sessions.sessionVersion` menjadi extension aplikasi;
- session actor/role/scope selalu dimuat ulang dari database; client tidak boleh menyuplai role atau department actor;
- cookie bersifat host-only, `HttpOnly`, `SameSite=Lax`, path `/`, dan `Secure` pada production.

## ADR-023 - Reset password memakai token hash aplikasi

Status: Accepted; menggantikan rancangan Phase 1 yang mengandalkan `verifications` untuk alur reset.

Phase 4 memakai tabel `password_reset_tokens` dengan random token 32 byte yang hanya dikirim melalui outbox development dan disimpan sebagai SHA-256 hash. Token ber-TTL, single-use, memiliki `usedAt`, dan token lama user diinvalidasi saat request/reset baru. Reset berhasil memperbarui hash Argon2id, membersihkan forced-password state, menaikkan `sessionVersion`, mencabut seluruh session, serta tidak melakukan auto-login.

Alasan perubahan: lifecycle single-use, replay test, request IP hash, bulk invalidation, dan audit lebih eksplisit daripada memaksakan contract `verifications`. Tabel `verifications` tetap bagian schema Better Auth tetapi bukan sumber kebenaran reset Phase 4.

## ADR-024 - Rate limit dan revocation dibuat atomik di PostgreSQL

Status: Accepted.

`auth_rate_limits` menyimpan hanya HMAC key gabungan scope + normalized identity + IP hash. `INSERT ... ON CONFLICT` menaikkan counter secara atomik agar request paralel tidak melampaui batas. Better Auth rate limit tetap aktif sebagai defense-in-depth, tetapi response dan batas alur aplikasi ditentukan service Phase 4.

Logout menghapus session saat ini. Revoke-all, password change/reset, dan account disable menaikkan `users.sessionVersion` serta menghapus session user; guard juga membandingkan versi session sehingga row lama tidak dapat digunakan bila deletion/replikasi terlambat.

## ADR-025 - Authorization selalu backend dan di luar scope menjadi 404

Status: Accepted.

Urutan guard: session valid -> expiry absolut/idle -> session version -> akun aktif/tidak banned -> role valid -> department constraint -> forced password -> permission/resource scope. `DEPT_PJ` wajib satu `departmentId` aktif, `SUPER_ADMIN` wajib tanpa department. Resource di luar Birdep PJ menghasilkan 404 untuk mengurangi enumeration; penolakan authorization ditulis ke audit tanpa password, cookie, raw IP, atau token.

## Environment variable standalone

Nama berikut adalah kontrak konfigurasi yang direncanakan. File `.env.example` hanya berisi nama dan dokumentasi, tanpa nilai production.

| Nama | Sensitif | Tujuan |
|---|---:|---|
| `NODE_ENV` | Tidak | Mode runtime |
| `NEXT_PUBLIC_APP_URL` | Tidak | Origin publik Sekolah |
| `DATABASE_URL` | Ya | Connection string aplikasi/runtime |
| `DIRECT_DATABASE_URL` | Ya | Connection langsung untuk migration bila provider memerlukannya |
| `TEST_DATABASE_URL` | Ya | Connection string database integration test |
| `AUTH_SECRET` | Ya | Secret signing/derivation auth aplikasi |
| `SESSION_COOKIE_NAME` | Tidak | Nama cookie session |
| `SESSION_MAX_AGE_SECONDS` | Tidak | Umur absolut session |
| `SESSION_IDLE_TIMEOUT_SECONDS` | Tidak | Hard idle timeout berdasarkan aktivitas session |
| `SESSION_UPDATE_AGE_SECONDS` | Tidak | Interval pembaruan rolling session Better Auth |
| `PASSWORD_RESET_TTL_SECONDS` | Tidak | Umur token reset |
| `TEMP_PASSWORD_TTL_SECONDS` | Tidak | Umur temporary password |
| `LOGIN_RATE_LIMIT_WINDOW_SECONDS` | Tidak | Window rate limit login |
| `LOGIN_RATE_LIMIT_MAX_ATTEMPTS` | Tidak | Batas percobaan per window |
| `PASSWORD_MAX_LENGTH` | Tidak | Batas password tanpa truncation |
| `SEED_SUPER_ADMIN_PASSWORD` | Ya | Password akun fixture Super Admin development |
| `SEED_DEPT_PJ_PASSWORD` | Ya | Password akun fixture PJ development |
| `IP_HASH_SECRET` | Ya | HMAC/hash IP pada audit/rate limit |
| `MINIO_ENDPOINT` | Tidak | URL endpoint MinIO self-hosted (ADR-035) |
| `MINIO_ACCESS_KEY` | Ya | Akses server-only ke private storage MinIO |
| `MINIO_SECRET_KEY` | Ya | Akses server-only ke private storage MinIO |
| `STORAGE_BUCKET_CANDIDATES` | Tidak | Nama bucket private kandidat |
| `STORAGE_SIGNED_URL_TTL_SECONDS` | Tidak | Umur URL unduh sementara |
| `RESEND_API_KEY` | Ya | Credential provider email Resend (ADR-036) |
| `RESEND_FROM_EMAIL` | Tidak | Sender terverifikasi di Resend |
| `CRON_SECRET` | Ya | Otorisasi worker/outbox/cleanup |
| `TURNSTILE_SECRET_KEY` | Ya | Abuse protection server bila diaktifkan |
| `NEXT_PUBLIC_TURNSTILE_SITE_KEY` | Tidak | Site key public bila diaktifkan |
| `SENTRY_DSN` | Ya | Error reporting server bila diaktifkan |
| `NEXT_PUBLIC_SENTRY_DSN` | Tidak | Error reporting client bila diaktifkan |
| `LOG_LEVEL` | Tidak | Tingkat structured logging |
| `REGISTRATION_SUBMISSION_ENABLED` | Tidak | Release gate pendaftaran; default `false` |
| `REGISTRATION_DRAFT_TTL_SECONDS` | Tidak | Umur draft teks di browser |
| `REGISTRATION_UPLOAD_TTL_SECONDS` | Tidak | Umur upload tervalidasi sebelum cleanup orphan |
| `REGISTRATION_CONFIRMATION_TTL_SECONDS` | Tidak | Umur token bukti pendaftaran |
| `MOTIVATION_MIN_WORDS` | Tidak | Batas minimum motivasi per pilihan |
| `ESSAY_MIN_WORDS` | Tidak | Batas minimum jawaban esai |
| `ESSAY_MAX_WORDS` | Tidak | Batas maksimum jawaban esai |
| `PORTFOLIO_MAX_FILES` | Tidak | Maksimum item portofolio |
| `PORTFOLIO_MAX_FILE_BYTES` | Tidak | Maksimum ukuran file portofolio |
| `PORTFOLIO_URL_MAX_LENGTH` | Tidak | Maksimum panjang URL HTTPS |
| `STORAGE_PRIVATE_ROOT` | Tidak | Subfolder privat adapter development |
| `EMAIL_SINK_ROOT` | Tidak | Subfolder email sink development |
| `EMAIL_OUTBOX_MAX_ATTEMPTS` | Tidak | Maksimum retry email outbox |

`SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`, `AUTH_SECRET`, dan secret lain hanya tersedia pada server/secret manager dan dilarang menggunakan prefix `NEXT_PUBLIC_`.

## Private storage production

**SUPERSEDED oleh ADR-035 (Phase 9).** Supabase Storage sebelumnya direkomendasikan untuk MVP; pemilik proyek memutuskan MinIO self-hosted di server Contabo milik Biro sebagai gantinya (S3-compatible, sama-sama mendukung bucket privat, signed upload/download berumur pendek, dan service credential server-side saja). Aplikasi tetap menyimpan metadata/ownership di PostgreSQL Sekolah; adapter (`MinioPrivateStorage`, `src/server/storage/private-storage.ts`) mengimplementasikan interface yang sama sehingga tetap dapat dipindahkan ke storage S3-compatible lain bila perlu.

## Keputusan yang masih blocking

### Blocking sebelum informasi atau fitur terkait boleh dipublikasikan

1. Konfirmasi nama resmi produk/organisasi dan identitas visual final.
2. Konfirmasi 13 unit, unit yang menerima peserta, status BPH, dan kuota per Birdep.
3. Daftar prodi resmi, tahun masuk untuk Angkatan 63, serta aturan eligibility final.
4. Tanggal periode, registration prefix/tahun, aturan Pilihan 2, dan kebijakan unlock.
5. Kebijakan privasi, versi consent, retensi, koreksi/penghapusan, dan owner data.

### Blocking sebelum fitur terkait diaktifkan

1. ~~Persetujuan project/provider storage production~~ **SELESAI (ADR-035, Phase 9)**: MinIO self-hosted Contabo, owner pemilik proyek sendiri.
2. ~~Sender domain, provider, template, volume, dan owner email production~~ **SELESAI (ADR-036, Phase 9)**: Resend, sender domain/template konkret masih perlu dikonfigurasi pemilik proyek di dashboard Resend sebelum smoke test pertama.
3. Identitas Super Admin awal dan kanal aman distribusi credential sebelum UAT auth.
4. Parameter session/reset/rate-limit final berdasarkan risk review sebelum production/UAT final.
5. ~~Owner domain, hosting, CI, backup, monitoring, dan on-call~~ **SELESAI (ADR-037/038, Phase 9)**: pemilik proyek sendiri, server Contabo milik Biro.

### Non-blocking/future

- CMS versus content-in-code untuk landing.
- Analytics non-esensial dan consent platform.
- Scheduling wawancara dan tracking publik.
- 2FA dan dual approval untuk aksi kritis.
- Integrasi SSO Nexus setelah MVP.
